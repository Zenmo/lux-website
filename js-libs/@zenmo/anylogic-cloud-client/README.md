
This is a copy of the code distributed with AnyLogic Cloud. The original can be found at https://cloud.anylogic.com/assets/js-client-8.5.0/cloud-client.js

It is wrapped so it can be used as a Node package and ECMAScript module.  

It needs to have 2 parent directories so the top directory can act as a NODE_PATH directory, like node_modules.
